import { handlers } from "../../../../auth"; // Import your handlers from your server auth.ts file

// Export the GET and POST handlers exactly as requested by Auth.js
export const { GET, POST } = handlers;
